/** Default LaTeX document shown for a fresh project. */
export const DEFAULT_DOCUMENT = String.raw`\documentclass{article}

\begin{document}

Hello World.

This document was compiled entirely inside your browser.

\end{document}
`

export const DEFAULT_PROJECT_NAME = 'Untitled Project'